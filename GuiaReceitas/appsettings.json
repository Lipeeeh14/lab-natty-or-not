{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "OpenAiSettings": {
    "ApiKey": "",
    "Prompt": "Gere um html contendo apenas o conteudo dentro da tag body (sem a tag body), mostrando uma receita simples com o seguinte ingrediente {ingrediente}. O resultado deve ser apenas o código sem retornar nenhum texto a mais"
  }
}
